# Decisions log — durable B↔T coordination decisions (append-only; one line of rationale each)

The mailbox (`from-*.md`) transports the discussion; lasting coordination decisions get recorded here so they
survive and stay findable. Reference the entry id(s) that settled it.

- 2026-06-19 (setup) — **Work split (PROVISIONAL — adjustable by mailbox agreement):** **B (Benjamin's Claude) owns the Simulation & Game Core** — deterministic tick/sim loop, entity model (ECS), units, economy/resources, combat, pathfinding, fog of war, and the netcode coupled to the sim. **T (Tien's Claude) owns Presentation, Platform & Tooling** — rendering/scene, camera & input, UI/HUD, audio, cross-platform (macOS + Windows) build & packaging, and the map/content pipeline. Rationale: a clean model↔view seam maximizes parallel work and minimizes merge conflicts. **The shared foundation — game engine, language, and multiplayer simulation model — is decided JOINTLY before either lane writes code** (kickoff: B-1).
- 2026-06-20 (B-3 ↔ T-6) — **FOUNDATION LOCKED (joint; Benjamin + Tien concur):** Engine = **Godot 4 (latest stable 4.x, Forward+)**; sim language = **C++ via GDExtension (godot-cpp + SCons)**; netcode = **deterministic lockstep** (input-delay N≈2–4; M0 single-player, structured so M3 transport is additive, not a rewrite). Sim determinism (hard): **fixed-point Q32.32** for ALL sim state (no float/double in sim, no Godot-side FP), one seeded PRNG/match, stable iteration order → bit-identical on macOS-arm64 + Windows-x64; replay = seed + command log; per-N-tick state hash = desync oracle + test target. EnTT ECS, fixed system order, ~24 Hz fixed tick (revisable). Rationale: C++ gives cross-platform determinism control; Godot gives free Mac+Win export + presentation stack; lockstep fits SC2-scale unit counts at tiny bandwidth.
- 2026-06-20 (B-3/B-4 ↔ T-6) — **Model↔view seam = double-buffered read-only POD snapshot** via C ABI `sim_abi.h` (AoS, ptr+len): per-entity { id, type, owner, state-bitflags, x,y (Q32.32), facing 0..65535, hp, hp_max } + per-match tick/count/resources[8]. **Snapshot buffer is valid only until the next `sim_advance()` → view COPIES into its own prev/curr buffers and interpolates** (B-5); entity ids stable & non-recycled within a match. Commands flow back as POD (`SimCommand`: MOVE/ATTACK/HARVEST/BUILD/TRAIN/STOP). B owns the POD contract; T owns the marshaling mechanism. Canonical header target: `sim/include/sim_abi.h` (to confirm in B's PR).
- 2026-06-20 (B-3/B-4 ↔ T-6) — **Repo bootstrap & layout:** **T scaffolds `main`** — top-level `sim/` [B] · `gdext/` [T, seam co-defined] · `game/` Godot project [T] · `tests/` [B] · `assets/` · `tools/` · `docs/`; plus GDExtension stub + CI matrix — and **B PRs `sim/`+`tests/` onto it**. Rationale: T owns build/CI/export so T lays the foundation both lanes build against; avoids racing the first commit. Game code/docs → `main` via feature-branch PRs; never merge `mailbox` into `main`.
- 2026-06-20 (B-7 ↔ T-8) — **Seam header finalized:** canonical path `sim/include/sim/sim_abi.h`, included as `#include <sim/sim_abi.h>` (add `sim/include` to the include path). `SimEntitySnapshot.state` canonical combinable bitflags: IDLE=0, MOVING, ATTACKING, HARVESTING, CARRYING, BUILDING, DEAD. Supersedes the provisional header location from T-6. **`main` scaffolded by T** (layout + gdext bridge + mock + Godot game + CI); **B PRs `sim/`+`tests/` onto it.** Real-sim link: SConstruct `use_real_sim=yes` (defines `SIM_RTS_USE_REAL_SIM`) globs `sim/src/*.cpp`, or links B's CMake `libsim` — finalized in B's PR. godot-cpp pinned to branch 4.5; CI = GitHub Actions (macOS-universal + Windows-x64) + `ctest --test-dir sim/build` determinism hook.
- 2026-06-20 (T-6) — **T-owned platform decisions:** Godot pin = latest stable 4.x (Forward+) · GDExtension build = godot-cpp + SCons · CI = GitHub Actions (macOS-universal arm64+x86_64 + Windows-x64, with a headless determinism-test hook) · ABI marshaling = ptr+len zero-copy at the C boundary (typed accessors to GDScript, not per-entity Variants) · placeholder art = primitives / Kenney CC0 · camera = edge-pan + WASD + scroll-zoom · controls = left-drag box-select + right-click context command → the 6 sim POD commands.
